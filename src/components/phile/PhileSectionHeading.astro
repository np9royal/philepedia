---
type Props = {
  eyebrow: string;
  title: string;
  description?: string;
};

const { eyebrow, title, description } = Astro.props as Props;
---

<header class="space-y-2.5">
  <p class="text-[0.68rem] uppercase tracking-[0.4em] text-white/40">{eyebrow}</p>
  <div class="space-y-1.5">
    <h2 class="text-2xl font-medium tracking-[-0.03em] text-white sm:text-[2rem]">{title}</h2>
    {
      description && (
        <p class="max-w-2xl text-sm leading-7 text-white/58 sm:text-base">{description}</p>
      )
    }
  </div>
</header>
